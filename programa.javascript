// Programa de prueba - Grupo 15
let int a;
let float b;

a = 1;
b = 10.5;

// Bucle for con asignación simple y con división (/=)
for (a = 1; a = a + 1; a = a + 1) {
    b /= 2;
}

// Cadenas con comillas simples
let string s;
s = 'Hola Mundo';

// Llamada a función y retorno
function void saludar(void) {
    return;
}